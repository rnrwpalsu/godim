<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8">
    <title>
        <%= title %>
    </title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
</head>

<body>
    <h1>
        <%= title %>
    </h1>
    <a href="/writeform">방명록 쓰기</a>
    <%
        for(var i = 0; i < rows.length; i++){
            var row = rows[i];
            console.log("row : ", row);
    %>
        <div>
            no :
            <%= row.no %>
                <br> name :
                <%= row.name %>
                    <br> title :
                    <%= row.title %>
                        <br> content :
                        <p>
                            <%= row.content %>
                        </p>
                        <br>
        </div>
        <hr>
        <%
        }
    %>
</body>

</html>