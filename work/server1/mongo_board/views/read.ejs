<!DOCTYPE html>
<html>
  <head>
  <meta charset="utf-8">
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
     <script src="https://code.jquery.com/jquery-1.11.2.min.js"></script>
     <script>
         $(function(){
            $("#modify").click(function(){
                location.href = "/update/<%=page%>/<%=doc.num%>";
            });
            $("#del").click(function(){
                $("#frm1").show();
                $("#span1").hide();
            });
            $("#cancel").click(function(){
                $("#frm1").hide();
                $("#span1").show();
            });
            $("#go_del").click(function(){
                if($("#passwd").val() == ""){
                    alert("비밀번호!!!");
                    $("#passwd").focus();
                    return;
                }
                frm1.submit();
            });              
         });
     </script>
  </head>
  <body>
    <h1><%= title %></h1>
    <a href="/list/1">리스트</a><br>
     <table width="400", border="1">
        <tr>
            <th width = "100">글번호</th>
            <td><%= doc.num %></td>
        </tr>
        <tr>
            <th>제목</th>
            <td><%= doc.title%></td>
        </tr>        
        <tr>
            <th>내용</th>
            <td><pre><%= doc.content %></pre></td>
        </tr>
        <tr>
            <th>이름</th>
            <td><%= doc.name %></td>
        </tr>        
        <tr>
            <th>날짜</th>
            <td><%= doc.myregdate %></td>
        </tr>
        <tr>
            <th>조회수</th>
            <td><%= doc.hit %></td>
        </tr>
        <tr>
            <td colspan = "2">
                <span id='span1'>
                    <input type="button" value="수정" id="modify">
                    <input type="button" value="삭제" id="del">
                </span>
                <form action="/delete" method="post" name="frm1" id="frm1" style="display : inline; display : none">
                    <input type="hidden" name="num" value="<%doc.num%>">
                    <input type="hidden" name="page" value="<%page%>">
                    비밀번호 : <input id="passwd" type="password" name="passwd" size="12" maxlength="20">
                    <input id="go_del" type="button" value="Del">
                    <input id="cancel" type="button" value="cancel">
                </form>
            </td>
        </tr>    
    </table>
  </body>
</html>
